/**
 * Copyright (c) Microsoft.  All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Warning: This code was generated by a tool.
// 
// Changes to this file may cause incorrect behavior and will be lost if the
// code is regenerated.

'use strict';

var should = require('should');
var util = require('util');
var _ = require('underscore');

var testUtils = require('../../../util/util');
var CLITest = require('../../../framework/arm-cli-test');
var utils = require('../../../../lib/util/utils');
var NetworkTestUtil = require('../../../util/networkTestUtil');
var tagUtils = require('../../../../lib/commands/arm/tag/tagUtils');
var networkUtil = new NetworkTestUtil();

var generatorUtils = require('../../../../lib/util/generatorUtils');
var profile = require('../../../../lib/util/profile');
var $ = utils.getLocaleString;

var testPrefix = 'arm-network-application-gateway-ssl-cert-tests',
  groupName = 'xplat-test-ssl-cert',
  location;
var index = 0;

var sslCertificates = {
  password: 'pswd',
  passwordNew: 'pswd',
  certFile: 'test/data/sslCert.pfx',
  certFileNew: 'test/data/cert02.pfx',
  name: 'sslCertificateName'
};

sslCertificates.virtualNetworkName = 'virtualNetworkName';
sslCertificates.subnetName = 'subnetName';
sslCertificates.applicationGatewayName = 'applicationGatewayName';

var subnet = {
  addressPrefix: '10.0.0.0/16',
  addressPrefixNew: '10.0.0.0/24'
};
var virtualNetwork = {
  location: 'westus'
};
var applicationGateway = {
  backendAddresses: '10.0.0.0',
  location: 'westus'
};

var requiredEnvironment = [{
  name: 'AZURE_VM_TEST_LOCATION',
  defaultValue: 'westus'
}];

describe('arm', function () {
  describe('network', function () {
    var suite, retry = 5;
    var hour = 60 * 60000;

    before(function (done) {
      this.timeout(hour);
      suite = new CLITest(this, testPrefix, requiredEnvironment);
      suite.setupSuite(function () {
        location = sslCertificates.location || process.env.AZURE_VM_TEST_LOCATION;
        groupName = suite.isMocked ? groupName : suite.generateId(groupName, null);
        sslCertificates.location = location;
        sslCertificates.group = groupName;
        sslCertificates.name = suite.isMocked ? sslCertificates.name : suite.generateId(sslCertificates.name, null);
        if (!suite.isPlayback()) {
          networkUtil.createGroup(groupName, location, suite, function () {
            var cmd = 'network vnet create -g {1} -n virtualNetworkName --location {location} --json'.formatArgs(virtualNetwork, groupName);
            testUtils.executeCommand(suite, retry, cmd, function (result) {
              result.exitStatus.should.equal(0);
              var output = JSON.parse(result.text);
              var cmd = 'network vnet subnet create -g {1} -n subnetName --address-prefix {addressPrefix} --vnet-name virtualNetworkName --json'.formatArgs(subnet, groupName);
              testUtils.executeCommand(suite, retry, cmd, function (result) {
                result.exitStatus.should.equal(0);
                var output = JSON.parse(result.text);
                var cmd = 'network application-gateway create -g {1} -n applicationGatewayName --servers {backendAddresses} --location {location} --vnet-name virtualNetworkName --subnet-name subnetName --json'.formatArgs(applicationGateway, groupName);
                testUtils.executeCommand(suite, retry, cmd, function (result) {
                  result.exitStatus.should.equal(0);
                  var output = JSON.parse(result.text);
                  done();
                });
              });
            });
          });
        } else {
          var subscriptionId = profile.current.getSubscription().id;
          done();
        }
      });
    });
    after(function (done) {
      this.timeout(hour);
      networkUtil.deleteGroup(groupName, suite, function () {
        suite.teardownSuite(done);
      });
    });
    beforeEach(function (done) {
      suite.setupTest(done);
    });
    afterEach(function (done) {
      suite.teardownTest(done);
    });

    describe('ssl certificates', function () {
      this.timeout(hour);
      it('create should create ssl certificates', function (done) {
        var cmd = 'network application-gateway ssl-cert create -g {group} -n {name} --cert-password {password} --cert-file {certFile} --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
        testUtils.executeCommand(suite, retry, cmd, function (result) {
          result.exitStatus.should.equal(0);
          var parentOutput = JSON.parse(result.text);
          parentOutput.name.should.equal('applicationGatewayName');
          var output = utils.findFirstCaseIgnore(parentOutput.sslCertificates, {name: 'sslCertificateName'});
          output.name.should.equal(sslCertificates.name);
          done();
        });
      });
      it('show should display ssl certificates details', function (done) {
        var cmd = 'network application-gateway ssl-cert show -g {group} -n {name} --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
        testUtils.executeCommand(suite, retry, cmd, function (result) {
          result.exitStatus.should.equal(0);
          var output = JSON.parse(result.text);
          output.name.should.equal(sslCertificates.name);
          done();
        });
      });
      it('set should update ssl certificates', function (done) {
        var cmd = 'network application-gateway ssl-cert set -g {group} -n {name} --cert-password {passwordNew} --cert-file {certFileNew} --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
        testUtils.executeCommand(suite, retry, cmd, function (result) {
          result.exitStatus.should.equal(0);
          var parentOutput = JSON.parse(result.text);
          parentOutput.name.should.equal('applicationGatewayName');
          var output = utils.findFirstCaseIgnore(parentOutput.sslCertificates, {name: 'sslCertificateName'});
          output.name.should.equal(sslCertificates.name);
          done();
        });
      });
      it('list should display all ssl certificates in resource group', function (done) {
        var cmd = 'network application-gateway ssl-cert list -g {group} --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
        testUtils.executeCommand(suite, retry, cmd, function (result) {
          result.exitStatus.should.equal(0);
          var outputs = JSON.parse(result.text);
          _.some(outputs, function (output) {
            return output.name === sslCertificates.name;
          }).should.be.true;
          done();
        });
      });
      it('delete should delete ssl certificates', function (done) {
        var cmd = 'network application-gateway ssl-cert delete -g {group} -n {name} --quiet --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
        testUtils.executeCommand(suite, retry, cmd, function (result) {
          result.exitStatus.should.equal(0);

          cmd = 'network application-gateway ssl-cert show -g {group} -n {name} --gateway-name {applicationGatewayName} --json'.formatArgs(sslCertificates);
          testUtils.executeCommand(suite, retry, cmd, function (result) {
            result.exitStatus.should.equal(0);
            var output = JSON.parse(result.text || '{}');
            output.should.be.empty;
            done();
          });
        });
      });
    });
  });
});
